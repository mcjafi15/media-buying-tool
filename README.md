# Hilco Global — Media Buying Tool

AI-powered media buying tool for Hilco's deal marketing team.

## Setup

1. `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and fill in API credentials
3. `streamlit run app.py`
